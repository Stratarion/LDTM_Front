/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'picsum.photos',
      'storage.googleapis.com',
      'appvolkov.storage.yandexcloud.net',
      'images.unsplash.com'
    ],
  },
}

module.exports = nextConfig 